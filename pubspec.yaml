name: mationani
description: aim to integrate all the simple animations into a stateful widget -- mationani
version: 0.0.2
repository: https://github.com/nomagicisreal/mationani

environment:
  sdk: ^2.17.0

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.1.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
